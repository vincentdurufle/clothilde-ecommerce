{% extends 'base.html.twig' %}
{% trans_default_domain 'app' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('admin_index') }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="menu">
            <a class="btn btn-lg btn-primary" href="">{{ 'admin.menu.see_all'|trans }}</a>
            <a class="btn btn-lg btn-primary" href="">{{ 'admin.menu.add_item'|trans }}</a>
            <a class="btn btn-lg btn-primary" href="">{{ 'admin.menu.invoices'|trans }}</a>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('admin_index') }}
{% endblock %}
